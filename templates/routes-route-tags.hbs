			<PrivateRoute path="/{{ camelCase (getPluralName yml) }}" component={{ curly true }}{{ pascalCase (getPluralName yml) }}{{ curly }} />
			<PrivateRoute path="/{{ camelCase (getSingularName yml) }}/:{{ camelCase (getSingularName yml) }}{{ pascalCase (getPKey yml) }}" component={{ curly true }}{{ pascalCase (getSingularName yml) }}Edit} />
$1